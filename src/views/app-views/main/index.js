import React, { lazy, Suspense } from "react";
// import { Switch, Route, Redirect } from "react-router-dom";
// import Loading from 'components/shared-components/Loading';

const Main = ({match}) => {
  
  return (
  <div>
    Main component worked!
  </div>
)};

export default Main;